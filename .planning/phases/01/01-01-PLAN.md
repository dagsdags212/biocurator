---
phase: 01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/biocurator/config/schema.py
  - src/biocurator/providers/base.py
  - src/biocurator/config/loader.py
  - src/biocurator/utils/retryable_exceptions.py
  - src/biocurator/utils/network.py
autonomous: true
requirements:
  - CFG-01
  - CFG-02
  - ERR-04

must_haves:
  truths:
    - "tenacity is available as a project dependency"
    - "RetryConfig dataclass exists with max_attempts, backoff_factor, max_delay, timeout fields"
    - "GlobalConfig has optional retry field"
    - "SearchConfig has optional retry dict field for per-database overrides"
    - "DatabaseConfig has optional retry field"
    - "Existing YAML configs without retry blocks parse without error (backward compatible)"
    - "ConfigLoader parses global retry block and per-database overrides from YAML"
    - "RETRYABLE_EXCEPTIONS tuple exists with network-level exception types"
    - "Custom @retry decorator is removed from utils/network.py"
  artifacts:
    - path: "src/biocurator/config/schema.py"
      provides: "RetryConfig dataclass + retry fields on GlobalConfig/SearchConfig"
      min_lines: 50
      contains: "class RetryConfig"
    - path: "src/biocurator/providers/base.py"
      provides: "retry field on DatabaseConfig"
      contains: "retry: RetryConfig | None"
    - path: "src/biocurator/config/loader.py"
      provides: "YAML parsing of retry blocks"
      contains: "raw_retry = data.get(\"retry\")"
    - path: "src/biocurator/utils/retryable_exceptions.py"
      provides: "Shared retryable exception classification"
      contains: "RETRYABLE_EXCEPTIONS"
    - path: "pyproject.toml"
      provides: "tenacity dependency"
      contains: "tenacity"
  key_links:
    - from: "ConfigLoader._parse"
      to: "RetryConfig.from_dict"
      pattern: "data.get.\"retry\""
    - from: "ConfigLoader._parse_job"
      to: "SearchConfig.retry"
      pattern: "RetryConfig.*per_db_retry"
    - from: "retryable_exceptions.RETRYABLE_EXCEPTIONS"
      to: "tenacity retry predicates"
      pattern: "RETRYABLE_EXCEPTIONS"
---

<objective>
**Foundation: Config Schema, RetryConfig, Exception Types, and tenancy Dependency**

Purpose: Create all shared infrastructure that Plan 02 and Plan 03 depend on — the RetryConfig dataclass, schema extensions, YAML parsing, retryable exception classification, tenacity dependency, and removal of the custom @retry decorator.

Output:
- `pyproject.toml` — tenacity added to dependencies
- `src/biocurator/config/schema.py` — RetryConfig + retry fields on GlobalConfig/SearchConfig
- `src/biocurator/providers/base.py` — retry field on DatabaseConfig
- `src/biocurator/config/loader.py` — parses retry blocks from YAML
- `src/biocurator/utils/retryable_exceptions.py` — new file with RETRYABLE_EXCEPTIONS tuple
- `src/biocurator/utils/network.py` — gutted (custom @retry removed)
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01/CONTEXT.md
@.planning/phases/01/RESEARCH.md
@.planning/phases/01/01-PATTERNS.md

**Existing files to read before modification:**
- `pyproject.toml` — current dependencies list, line 30-38
- `src/biocurator/config/schema.py` — current dataclass definitions
- `src/biocurator/providers/base.py` — current DatabaseConfig at lines 94-101
- `src/biocurator/config/loader.py` — current `_parse()` and `_parse_job()` methods
- `src/biocurator/utils/network.py` — current `@retry` decorator to gut
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tenacity dependency, RetryConfig dataclass, and retry fields to schema</name>
  <read_first>
    - pyproject.toml (lines 30-38: current dependencies list)
    - src/biocurator/config/schema.py (all: current dataclass definitions)
    - src/biocurator/providers/base.py (lines 94-101: DatabaseConfig)
    - PATTERNS.md (RetryConfig dataclass shape, GlobalConfig/SearchConfig retry field additions, DatabaseConfig retry field addition)
    - RESEARCH.md (RetryConfig dataclass design with None-sentinel merge, lines 598-616; Config Schema Changes section)
  </read_first>
  <files>
    pyproject.toml,
    src/biocurator/config/schema.py,
    src/biocurator/providers/base.py
  </files>
  <action>
    **A. pyproject.toml:** 
    Add `"tenacity>=9.1,<10.0",` to the `dependencies` list following existing alphabetical order — insert between pyyaml and typer entries at line 37.

    **B. src/biocurator/config/schema.py:**
    Add `RetryConfig` dataclass with the None-sentinel merge pattern from RESEARCH.md (lines 598-616):
    - Fields: `max_attempts: int | None = None`, `backoff_factor: float | None = None`, `max_delay: int | None = None`, `timeout: int | None = None`
    - Method `resolve(self, defaults: RetryConfig | None = None) -> RetryConfig`: resolve None fields with defaults then tenacity fallbacks (max_attempts=3, backoff_factor=2.0, max_delay=60, timeout=30)
    - Method `from_dict(cls, data: dict | None) -> RetryConfig | None`: parse from YAML dict, return None if empty/missing
    - Classmethod `defaults(cls) -> RetryConfig`: returns `cls().resolve()` — a fully-resolved RetryConfig with all tenacity defaults
    - Add `retry: RetryConfig | None = None` to `GlobalConfig`
    - Add `retry: dict[str, RetryConfig] | None = None` to `SearchConfig` (import field from dataclasses for typing, or `None` default directly)

    **C. src/biocurator/providers/base.py:**
    - Add import: `from biocurator.config.schema import RetryConfig`
    - Add `retry: RetryConfig | None = None` to `DatabaseConfig`
  </action>
  <acceptance_criteria>
    - pyproject.toml contains `tenacity>=9.1,<10.0` in dependencies
    - schema.py exports `RetryConfig` dataclass with fields max_attempts, backoff_factor, max_delay, timeout
    - `RetryConfig.resolve()` returns a resolved RetryConfig with defaults filled (never None fields)
    - `RetryConfig.defaults()` returns a fully resolved RetryConfig (no None fields) with tenacity defaults
    - `RetryConfig.from_dict(None)` returns None; `RetryConfig.from_dict({"max_attempts": 5})` returns RetryConfig(max_attempts=5, ...)
    - GlobalConfig has `retry: RetryConfig | None = None` field
    - SearchConfig has `retry: dict[str, RetryConfig] | None = None` field
    - DatabaseConfig has `retry: RetryConfig | None = None` field
    - `uv run pytest tests/config/test_schema.py -x` passes (existing tests unaffected)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/config/test_schema.py -x && python -c "from biocurator.config.schema import RetryConfig; r=RetryConfig(max_attempts=5); assert r.resolve().max_attempts == 5; d=RetryConfig.defaults(); assert d.max_attempts == 3"</automated>
  </verify>
  <done>
    tenacity in pyproject.toml, RetryConfig dataclass + retry fields on all schema types, verify command passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ConfigLoader to parse retry blocks from YAML</name>
  <read_first>
    - src/biocurator/config/loader.py (all: current _parse and _parse_job methods)
    - PATTERNS.md (ConfigLoader retry parsing patterns, lines 96-143)
    - RESEARCH.md (Config Schema Changes — loader.py impact section, lines 679-694)
  </read_first>
  <files>
    src/biocurator/config/loader.py
  </files>
  <action>
    **A. Import RetryConfig:**
    Add `RetryConfig` to the imports from `biocurator.config.schema` — alongside existing ExportConfig, FilterConfig, GlobalConfig, JobConfig, SearchConfig.

    **B. Update `_parse()` method:**
    After line 36 (`jobs = [...]`), add:
    ```python
    raw_retry = data.get("retry")
    retry_cfg = RetryConfig.from_dict(raw_retry) if raw_retry else None
    ```
    Then change the return to `GlobalConfig(email=email, jobs=jobs, retry=retry_cfg)`.

    **C. Update `_parse_job()` method:**
    After building `search_cfg` (after line 61), add per-database retry overrides parsing:
    ```python
    raw_job_retry = search_data.get("retry")
    if raw_job_retry and isinstance(raw_job_retry, dict):
        per_db_retry = {}
        for db_name, db_retry_cfg in raw_job_retry.items():
            per_db_retry[db_name] = RetryConfig.from_dict(db_retry_cfg)
        search_cfg.retry = per_db_retry
    ```

    **D. Backward compatibility verification:**
    Existing fixtures in `tests/fixtures/valid_config.yaml` have no `retry:` blocks. Loading them must produce `cfg.retry is None` and `cfg.jobs[0].search.retry is None` — no error.
  </action>
  <acceptance_criteria>
    - ConfigLoader._parse parses top-level `retry:` block into GlobalConfig.retry as RetryConfig
    - ConfigLoader._parse_job parses `search.retry:` per-database blocks into SearchConfig.retry dict
    - Existing fixtures/valid_config.yaml (no retry block) loads with retry=None — no error
    - `uv run pytest tests/config/test_loader.py -x` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/config/test_loader.py -x && python -c "from biocurator.config.loader import ConfigLoader; cfg=ConfigLoader.load('tests/fixtures/valid_config.yaml'); assert cfg.retry is None; assert cfg.jobs[0].search.retry is None"</automated>
  </verify>
  <done>
    ConfigLoader parses retry blocks, existing tests pass unchanged, backward compatibility confirmed
  </done>
</task>

<task type="auto">
  <name>Task 3: Create retryable_exceptions.py and gut utils/network.py</name>
  <read_first>
    - PATTERNS.md (retryable_exceptions.py design, lines 596-639; network.py removal, lines 485-516)
    - RESEARCH.md (Exception Classification Tables, lines 503-528; retry predicate pattern, lines 627-639)
    - src/biocurator/utils/network.py (all: current @retry decorator)
  </read_first>
  <files>
    src/biocurator/utils/retryable_exceptions.py,
    src/biocurator/utils/network.py
  </files>
  <action>
    **A. Create `src/biocurator/utils/retryable_exceptions.py`:**
    New file containing:
    - Imports: `import socket`, `import urllib.error`, `import requests`
    - `RETRYABLE_EXCEPTIONS` tuple with: `urllib.error.URLError`, `urllib.error.HTTPError`, `requests.ConnectionError`, `requests.Timeout`, `requests.HTTPError`, `socket.timeout`, `socket.gaierror`
    - `_is_retryable_http_error(exc)` function: for `requests.HTTPError`, check `exc.response is not None and exc.response.status_code >= 500`; for `urllib.error.HTTPError`, check `exc.code >= 500`; otherwise return False for non-HTTPError types
    - `RETRYABLE_PREDICATE` using tenacity's `retry_if_exception_type(RETRYABLE_EXCEPTIONS)` — note: this is a module-level predicate that can be chained with `_is_retryable_http_error` if needed. For now, define both `RETRYABLE_EXCEPTIONS` and a `retry_if_exception_type(RETRYABLE_EXCEPTIONS)` called `RETRYABLE_PREDICATE`.
    - Use standard project logger pattern: `from biocurator.utils.logging import get_logger`

    **B. Gut `src/biocurator/utils/network.py`:**
    - Keep the module docstring and copyright
    - Remove the entire `@retry` decorator function (lines 21-80)
    - Remove all imports that are no longer needed (time, functools, random, Callable/Type/Tuple/Any)
    - Keep the logger import (`from biocurator.utils.logging import get_logger` and `logger = get_logger(__name__)`)
    - Add a comment at the top: `# NOTE: Custom @retry removed in Phase 1. See utils/retryable_exceptions.py for exception types and tenacity for retry logic.`
    - Export an empty `__all__` list if keeping the file
  </action>
  <acceptance_criteria>
    - retryable_exceptions.py exists with `RETRYABLE_EXCEPTIONS` tuple containing all 7 network exception types
    - retryable_exceptions.py exports `_is_retryable_http_error` function that returns True for 5xx, False for 4xx and non-HTTPError
    - retryable_exceptions.py imports without errors
    - network.py no longer has `def retry(` — the decorator is removed
    - network.py still exists (not deleted) with the deprecation comment
    - `uv run pytest tests/utils/test_network.py -x` — tests currently test the old @retry decorator; they will now fail. This is expected — the old test file will be updated in Plan 02 to test tenacity.
  </acceptance_criteria>
  <verify>
    <automated>python -c "from biocurator.utils.retryable_exceptions import RETRYABLE_EXCEPTIONS, _is_retryable_http_error; import requests; assert not _is_retryable_http_error(requests.HTTPError(response=type('R',(),{'status_code':400})()))" 2>/dev/null; python -c "from biocurator.utils.retryable_exceptions import RETRYABLE_EXCEPTIONS; assert len(RETRYABLE_EXCEPTIONS) == 7"</automated>
  </verify>
  <done>
    retryable_exceptions.py created with exception types, network.py gutted, imports verified
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries introduced — this plan modifies configuration parsing and utility code only. No new input surfaces, no credential handling.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | S (Spoofing) | N/A | accept | This phase has no authentication or identity components |
| T-01-02 | T (Tampering) | YAML config parsing | accept | YAML is a local trusted file; PyYAML safe_load is already used |
| T-01-03 | I (Information Disclosure) | N/A | accept | No credentials/logic changes in this plan |
| T-01-04 | D (Denial of Service) | Retry config | mitigate | RetryConfig resolve() caps max_attempts at tenacity defaults; tenacity's stop_after_attempt prevents infinite retries |
</threat_model>

<verification>
1. `uv run pytest tests/config/test_schema.py -x` passes
2. `uv run pytest tests/config/test_loader.py -x` passes
3. `python -c "from biocurator.config.schema import RetryConfig, GlobalConfig, SearchConfig; from biocurator.providers.base import DatabaseConfig"` imports cleanly
4. `python -c "from biocurator.utils.retryable_exceptions import RETRYABLE_EXCEPTIONS, RETRYABLE_PREDICATE"` imports cleanly
5. `python -c "from biocurator.utils.network import retry"` raises ImportError (decorator removed)
</verification>

<success_criteria>
- tenacity>=9.1 is in pyproject.toml dependencies
- RetryConfig, GlobalConfig.retry, SearchConfig.retry, DatabaseConfig.retry all defined
- ConfigLoader parses retry blocks from YAML
- Existing configs without retry parse without error
- RETRYABLE_EXCEPTIONS tuple available for import
- Custom @retry decorator removed from utils/network.py
</success_criteria>

<output>
After completion, create `.planning/phases/01/01-01-PLAN-SUMMARY.md`
</output>

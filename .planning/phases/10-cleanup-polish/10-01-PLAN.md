---
phase: 10-cleanup-polish
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/biocurator/utils/network.py
  - .planning/phases/05-pre-flight-check-integration/05-HUMAN-UAT.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Orphaned `utils/network.py` stub is deleted from the codebase"
    - "Phase 05 human UAT document reflects completed live API smoke test"
  artifacts:
    - path: "src/biocurator/utils/network.py"
      provides: "Orphaned stub — to be deleted"
      min_lines: 0
      deleted: true
    - path: ".planning/phases/05-pre-flight-check-integration/05-HUMAN-UAT.md"
      provides: "Updated UAT with completed test result"
      contains: "passed: 1"
  key_links:
    - from: "src/biocurator/utils/network.py"
      to: "None (deleted)"
      via: "deletion"
      pattern: "rm.*network.py"
---

<objective>
Delete the orphaned `utils/network.py` stub and complete the Phase 05 human UAT document with the live API smoke test results.

Purpose: Clean up dead code and close out the Phase 05 UAT.
Output: Clean codebase, completed UAT document.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-pre-flight-check-integration/05-HUMAN-UAT.md
@src/biocurator/utils/network.py
</context>

<interfaces>
<!-- No new interfaces created — this is a cleanup phase -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Delete orphaned `utils/network.py` stub</name>
  <files>src/biocurator/utils/network.py</files>
  <action>
Delete the file `src/biocurator/utils/network.py`. This file contains only a docstring and a NOTE comment (13 lines total) and has no imports anywhere in the codebase. It is a remnant from an earlier implementation that has been superseded by `utils/retryable_exceptions.py`.

Steps:
1. Confirm no imports exist: `grep -r "from.*utils.network\|import.*utils.network" --include="*.py" src/`
2. Delete the file: `rm src/biocurator/utils/network.py`
3. If the `utils/` directory becomes empty after deletion, remove the empty directory as well
  </action>
  <verify>
    <automated>
# Verify file is deleted
test ! -f src/biocurator/utils/network.py && echo "DELETED" || echo "STILL EXISTS"
# Verify no imports remain
grep -r "utils.network" --include="*.py" src/ 2>/dev/null | head -5 || echo "NO IMPORTS"
    </automated>
  </verify>
  <done>
`src/biocurator/utils/network.py` does not exist. No Python files reference it. If the `utils/` directory is empty, it is removed.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Complete Phase 05 human UAT — Live API Smoke Test</name>
  <files>.planning/phases/05-pre-flight-check-integration/05-HUMAN-UAT.md</files>
  <action>
Run the Phase 05 human UAT live API smoke test and update the UAT document.

**Test to perform:**
```bash
uv run biocurator run biocurator_config.yaml --check
```

**Expected behavior (from UAT document):**
- "Pre-flight Health Check" Rich table renders with actual provider statuses (UP/DOWN), real response times (in ms), and current breaker states
- All providers UP → "All providers reachable. Proceeding with job execution." → job runs
- Any provider DOWN → warning message + "Proceed anyway?" interactive prompt
- `--no-check` skips the table entirely
- Table styling matches `biocurator status` output (same columns, colors, header style)

**After running the test:**
1. Update `.planning/phases/05-pre-flight-check-integration/05-HUMAN-UAT.md`:
   - Set `status: complete` in frontmatter
   - Set `result: passed` or `result: failed` for test #1
   - Add actual observed output in a code block
2. Update the Summary section:
   - `passed: 1` (or `passed: 0` if failed)
   - `pending: 0`
  </action>
  <verify>
    <automated>
# Check UAT document has been updated
grep -E "status:|result:|passed:|pending:" .planning/phases/05-pre-flight-check-integration/05-HUMAN-UAT.md | head -10
    </automated>
  </verify>
  <done>
Phase 05 human UAT document shows `status: complete`, `passed: 1`, `pending: 0`. The live API smoke test has been run and results are documented.
  </done>
</task>

</tasks>

<verification>
## Phase-level Verification

1. **File deletion verified:**
   ```bash
   test ! -f src/biocurator/utils/network.py && echo "PASS" || echo "FAIL"
   ```

2. **UAT completion verified:**
   ```bash
   grep -E "^status: complete$|^passed: 1$" .planning/phases/05-pre-flight-check-integration/05-HUMAN-UAT.md
   ```
</verification>

<success_criteria>
- [ ] `src/biocurator/utils/network.py` is deleted
- [ ] No imports of `utils.network` remain in the codebase
- [ ] Phase 05 UAT document shows `status: complete` and `passed: 1`
- [ ] Both success criteria from ROADMAP.md are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/10-cleanup-polish/10-01-SUMMARY.md`
</output>

---
phase: 06-retrospective-verification
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-cli-jobs-files-commands/04-VERIFICATION.md
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03

must_haves:
  truths:
    - "04-VERIFICATION.md confirms CLI-01: biocurator jobs command lists jobs from config with Rich table"
    - "04-VERIFICATION.md confirms CLI-02: biocurator files command lists downloaded files with manifest metadata"
    - "04-VERIFICATION.md confirms CLI-03: biocurator files --verify re-checks SHA-256 checksums against disk"
    - "All Phase 4 tests (9+ for files, 5 for jobs) pass at time of verification"
  artifacts:
    - path: ".planning/phases/04-cli-jobs-files-commands/04-VERIFICATION.md"
      provides: "Formal verification report for Phase 04"
      contains: "## Requirements Coverage"
  key_links:
    - from: "04-VERIFICATION.md"
      to: "src/biocurator/cli/commands/jobs.py"
      via: "Code evidence citations"
      pattern: "cli/commands/jobs\\.py"
    - from: "04-VERIFICATION.md"
      to: "src/biocurator/cli/commands/files.py"
      via: "Code evidence citations"
      pattern: "cli/commands/files\\.py"
    - from: "04-VERIFICATION.md"
      to: "src/biocurator/cli/main.py"
      via: "Command registration evidence"
      pattern: "app\\.command"
---

<objective>
Create 04-VERIFICATION.md — a formal retrospective audit confirming Phase 04 (CLI Jobs & Files Commands) code satisfies all three assigned requirements: CLI-01, CLI-02, CLI-03.

Purpose: Close the missing-VERIFICATION.md gap identified by the milestone audit. All Phase 4 code was already built and tested — this plan audits it retroactively against the ROADMAP success criteria.

Output: `.planning/phases/04-cli-jobs-files-commands/04-VERIFICATION.md` with YAML frontmatter, Observable Truths table, Requirements Coverage table, Key Link Verification, Test Results, and Anti-Patterns/Gaps sections.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-cli-jobs-files-commands/04-01-SUMMARY.md
@.planning/phases/04-cli-jobs-files-commands/04-02-SUMMARY.md
@.planning/phases/04-cli-jobs-files-commands/04-03-SUMMARY.md
@.planning/phases/02-circuit-breaker-health-status/02-VERIFICATION.md
@.planning/phases/03-checksums-manifests/03-VERIFICATION.md

<interfaces>
<!-- Reference formats from existing VERIFICATION.md files -->

From `02-VERIFICATION.md` and `03-VERIFICATION.md` — template structure:
```yaml
# YAML frontmatter
---
phase: <phase-slug>
status: passed | gaps_found | failed
verified: <ISO-8601 date>
must_haves_verified: <N>
must_haves_total: <N>
requirements_covered: [<REQ-ID>, ...]
gaps: []
human_verification: []
---
```

From ROADMAP Phase 4 success criteria (what verification MUST prove):
1. `biocurator jobs` auto-detects `biocurator_config.yaml` in CWD; shows clear error if missing
2. `biocurator files my_job` lists files with metadata from manifest (path, format, size, checksum, record count)
3. `biocurator files --verify` recomputes checksums, compares against manifest, reports corruption
4. `biocurator files` without job name shows all jobs with downloaded data
5. All outputs use Rich tables with `header_style="bold magenta"` and consistent formatting

Key code relationships verification must trace:
- `jobs_command` → `ConfigLoader.load()` → `GlobalConfig.jobs` → Rich Table
- `files_command` (list) → `ConfigLoader.load()` → manifest.json → Rich Table (single) or summary (all)
- `files_command` (verify) → `manifest_verify()` from `biocurator.core` → per-file status table
- Both commands registered via `app.command()` in `main.py`
- Default config filename: `biocurator_config.yaml`
- Exit codes: 1 for errors (config not found, unknown job, corrupted files)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task: Audit Phase 04 codebase and create 04-VERIFICATION.md</name>
  <files>.planning/phases/04-cli-jobs-files-commands/04-VERIFICATION.md</files>

  <read_first>
Read these files IN ORDER before writing anything:
1. `.planning/phases/04-cli-jobs-files-commands/04-01-SUMMARY.md` — what was built in Plan 04-01 (jobs command)
2. `.planning/phases/04-cli-jobs-files-commands/04-02-SUMMARY.md` — what was built in Plan 04-02 (files list mode)
3. `.planning/phases/04-cli-jobs-files-commands/04-03-SUMMARY.md` — what was built in Plan 04-03 (files --verify)
4. `src/biocurator/cli/commands/jobs.py` — CLI-01 implementation (full source ~57 lines)
5. `src/biocurator/cli/commands/files.py` — CLI-02 + CLI-03 implementation (full source ~261 lines)
6. `src/biocurator/cli/main.py` — command registrations (jobs + files)
7. `src/biocurator/core/verifier.py` — manifest_verify() library function (CLI-03 dependency)
8. `src/biocurator/core/__init__.py` — manifest_verify export
9. `src/biocurator/config/loader.py` — ConfigLoader.load() used by both commands
10. `tests/cli/test_jobs.py` — jobs command tests
11. `tests/cli/test_files.py` — files command tests (9 passed: 5 list-mode + 4 verify-mode)
12. `.planning/phases/02-circuit-breaker-health-status/02-VERIFICATION.md` — reference format template
13. `.planning/phases/03-checksums-manifests/03-VERIFICATION.md` — reference format template
  </read_first>

  <action>
**Purpose:** Perform a thorough code audit of Phase 04 deliverables and write a formal VERIFICATION.md.

**Step 1: Run Phase 4 tests**
```bash
uv run pytest tests/cli/test_jobs.py tests/cli/test_files.py -q --tb=short
uv run pytest tests/ -q --tb=short
```
Record pass counts. Expected: 5 passed (jobs), 9 passed (files), ≥190 passed (full suite).

**Step 2: Verify each requirement with grep-level evidence**

CLI-01 (biocurator jobs command):
- Verify jobs_command exists: `def jobs_command` in `src/biocurator/cli/commands/jobs.py` line 15
- Verify it accepts `--config` / `-c` option: grep for `typer.Option.*--config.*-c` in jobs.py — should be at line 17
- Verify default config filename: `= "biocurator_config.yaml"` in jobs.py line 19
- Verify it loads config: grep `ConfigLoader.load(config)` in jobs.py — should be at line 23
- Verify it handles ConfigNotFoundError: grep `ConfigNotFoundError` in jobs.py — should be at line 24-28, shows `--config` hint and exits 1
- Verify it handles InvalidConfigError: grep `InvalidConfigError` in jobs.py — should be at line 29-31, exits 1
- Verify Rich table rendered: grep `Table\(` in jobs.py — should be at line 33 with title="Curation Jobs"
- Verify columns: Job Name, Databases, Organism, Max Results, Output Dir, Formats — all 6 columns in add_column calls
- Verify header_style: grep `"bold magenta"` in jobs.py — should be at line 37
- Verify job list display: `for job in global_config.jobs:` iterates all jobs
- Verify registered in main.py: grep `app.command("jobs")` in `src/biocurator/cli/main.py` — should be at line 50

CLI-02 (biocurator files list mode):
- Verify files_command exists: `def files_command` in `src/biocurator/cli/commands/files.py` line 26
- Verify job_name argument (optional): grep `Optional\[str\]` and `typer.Argument` in files.py — should be at line 27-30
- Verify `--config` / `-c` option: grep `typer.Option.*--config.*-c` in files.py — should be at line 31-34
- Verify `--verify` flag: grep `typer.Option.*--verify` in files.py — should be at line 35-38
- Verify default config: `= "biocurator_config.yaml"` in files.py line 34
- Verify both config error handlers: ConfigNotFoundError and InvalidConfigError caught with rich output + typer.Exit(1)
- Verify single-job list mode (_handle_single_job_list): checks job existence, reads manifest.json, renders per-file table with Filename/Format/Size/Records/SHA-256 columns
- Verify sha256 display: `entry["sha256"][:12] + "..."` — truncated SHA-256 prefix in output
- Verify all-jobs summary mode (_handle_all_jobs_summary): iterates all jobs, checks manifest existence, renders summary table with Job/Output Dir/Files/Records/Manifest columns
- Verify no-data message: `"No downloaded data found. Run jobs with \`biocurator run\` first."` — yellow output
- Verify unknown job handling: exits 1 with available job names listed
- Verify corrupt manifest handling: catches json.JSONDecodeError / OSError, shows "[bold red]corrupt[/bold red]"
- Verify header_style: grep `"bold magenta"` in files.py — should appear multiple times (list table, verify table)
- Verify registered in main.py: grep `app.command("files")` in `src/biocurator/cli/main.py` — should be at line 51

CLI-03 (biocurator files --verify):
- Verify --verify dispatch: `if verify: _handle_verify(job_name, global_config, config)` in files.py line 50-52
- Verify _verify_one_job() calls manifest_verify: grep `manifest_verify(` in files.py — should be at line 216
- Verify manifest_verify imported: grep `from biocurator.core import manifest_verify` in files.py — should be at line 14
- Verify per-file status rendering: ok (✓ green), corrupted (✗ red), missing (? yellow) — lines 236-249
- Verify all_ok branch: `if result["all_ok"]: console.print("[bold green]All checksums verified ✓[/bold green]")` in files.py line 253-254
- Verify exit codes: if any failure → `raise typer.Exit(1)` in verify paths (lines 183, 204, 222)
- Verify no-manifest message: `"No manifest for '{job_name}' — run the job first."` — yellow, no error exit (line 211-214)
- Verify all-jobs verify: iterates jobs_with_manifests, runs _verify_one_job on each, aggregates failure
- Verify manifest_verify function in verifier.py: exists, handles json.JSONDecodeError, uses chunked reads (8192 bytes), rejects path traversal, returns structured result dict with all expected keys
- Verify manifest_verify export: in `src/biocurator/core/__init__.py` — `from biocurator.core.verifier import manifest_verify` + listed in `__all__`

**Step 3: Check for anti-patterns specific to Phase 04**
- Verify that files_command handles manifest corruption gracefully (not crashing, showing user-friendly message)
- Verify that no hardcoded paths exist (all paths derived from config)
- Verify that exit codes distinguish real errors (1) from informational exits (0 for "no data yet")
- Check that the --verify mode doesn't accidentally trigger when `--verify` is NOT passed
- Verify the import chain: files.py → biocurator.core → verifier.py (no circular imports)

**Step 4: Write 04-VERIFICATION.md**

Use this exact structure:

```markdown
---
phase: "04-cli-jobs-files-commands"
status: passed  # or: gaps_found if any issue found
verified: <current-ISO-8601-date>
must_haves_verified: <N>
must_haves_total: 3
requirements_covered: [CLI-01, CLI-02, CLI-03]
gaps: []
human_verification: []
---

# Phase 04: CLI Jobs & Files Commands — Verification Report

**Phase Goal:** Users can list available jobs from a config and inspect downloaded files with integrity verification.

**Status:** ✅ PASSED — all 3 must-haves verified against the live codebase.

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `biocurator jobs` auto-detects config and lists all jobs with Rich table | ✓ VERIFIED | <concrete code evidence with line numbers from jobs.py> |
| 2 | `biocurator files my_job` lists downloaded files with metadata from manifest | ✓ VERIFIED | <concrete code evidence from files.py list mode> |
| 3 | `biocurator files` (no job) shows all-jobs summary with manifest presence | ✓ VERIFIED | <concrete code evidence from _handle_all_jobs_summary> |
| 4 | `biocurator files --verify` recomputes checksums and reports corruption | ✓ VERIFIED | <concrete code evidence from _handle_verify / _verify_one_job> |
| 5 | All outputs use Rich tables with `header_style="bold magenta"` | ✓ VERIFIED | <grep evidence from both command files> |

**Score:** 5/5 truths verified

---

## Requirements Coverage

### ✅ CLI-01 — `biocurator jobs` command

**Evidence:**
- `src/biocurator/cli/commands/jobs.py` line 15: `def jobs_command(config=...)` with --config/-c option defaulting to `biocurator_config.yaml`
- Line 23: `global_config = ConfigLoader.load(config)` — loads config from path
- Lines 24-31: ConfigNotFoundError → "specify a config path with --config" hint + typer.Exit(1); InvalidConfigError → error message + typer.Exit(1)
- Lines 33-56: Rich Table with 6 columns (Job Name/Databases/Organism/Max Results/Output Dir/Formats), `header_style="bold magenta"`
- Line 45-53: Iterates `global_config.jobs`, renders each row
- Line 57: Footer shows job count
- `src/biocurator/cli/main.py` line 50: `app.command("jobs")(jobs_command)` — registered as CLI subcommand
- `tests/cli/test_jobs.py`: 5 tests covering listing, empty config, missing config, invalid config

### ✅ CLI-02 — `biocurator files` list mode

**Evidence:**
- `src/biocurator/cli/commands/files.py` line 26: `def files_command(job_name=None, config="biocurator_config.yaml", verify=False)` — optional job name, config, verify flag
- Lines 42-58: Config loading with error handling, verify dispatch vs list dispatch
- `_handle_single_job_list()` (lines 68-116): 
  - Line 70-76: Job validation with available job listing on unknown name + typer.Exit(1)
  - Lines 78-83: No-manifest check with yellow hint
  - Lines 86-89: Corrupt manifest detection (json.JSONDecodeError / OSError) + typer.Exit(1)
  - Lines 91-116: Rich Table with Filename/Format/Size/Records/SHA-256 columns, `header_style="bold magenta"`
  - Line 108: SHA-256 display: `entry["sha256"][:12] + "..."` (truncated for readability)
  - Lines 112-116: Summary footer with file count, record count, generation timestamp
- `_handle_all_jobs_summary()` (lines 119-167):
  - Lines 121-131: Rich Table with Job/Output Dir/Files/Records/Manifest columns
  - Lines 133-161: Per-job iteration: manifest exists → show stats; corrupt → show "corrupt"; missing → show "none"
  - Lines 164-167: No-data warning in yellow
- `src/biocurator/cli/main.py` line 51: `app.command("files")(files_command)` — registered as CLI subcommand
- `tests/cli/test_files.py`: 5 list-mode tests (plus 4 verify-mode tests = 9 total)

### ✅ CLI-03 — `biocurator files --verify`

**Evidence:**
- `src/biocurator/cli/commands/files.py` line 14: `from biocurator.core import manifest_verify` — imports verification function
- Lines 50-52: `if verify: _handle_verify(job_name, global_config, config); return` — dispatch
- `_handle_verify()` (lines 170-204):
  - Single-job (lines 172-183): validates job, calls `_verify_one_job()`, exits 1 on failure
  - All-jobs (lines 184-204): collects jobs with manifests, iterates, aggregates failures, exits 1 if any failure
- `_verify_one_job()` (lines 207-261):
  - Lines 210-214: No-manifest check → yellow hint, return False (no error)
  - Line 216: `result = manifest_verify(manifest_path)` — delegates to core library
  - Lines 218-222: Invalid manifest → red error + typer.Exit(1)
  - Lines 224-249: Per-file Rich Table with Filename/Status/SHA-256 Match/Notes, color-coded: ✓ green (ok), ✗ red (corrupted), ? yellow (missing)
  - Line 253-254: All checks pass → green "All checksums verified ✓"
  - Lines 257-261: Issues found → red summary with corrupted/missing counts
  - Returns True if any failure (causes typer.Exit(1) upstream)
- `src/biocurator/core/verifier.py` line 22: `def manifest_verify(manifest_path: Path) -> dict[str, Any]` — library function
  - Lines 46-59: Handles JSONDecodeError/OSError gracefully → returns `manifest_valid: False`
  - Lines 61-108: For each file entry: resolves path (security: rejects `..` traversal), checks existence, recomputes SHA-256 with chunked 8192-byte reads, compares
  - Lines 112-121: Returns structured dict with `all_ok`, `files_checked`, `files_matched`, `files_missing`, `files_corrupted`, `results`
- `src/biocurator/core/__init__.py` line 1: `from biocurator.core.verifier import manifest_verify` + listed in `__all__`
- `tests/cli/test_files.py`: 4 verify-mode tests covering corrupt files, missing files, all-ok, and no-manifest

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `jobs_command` | `ConfigLoader.load()` | `global_config = ConfigLoader.load(config)` | ✓ WIRED | jobs.py line 23 |
| `jobs_command` | `main.py app` | `app.command("jobs")(jobs_command)` | ✓ WIRED | main.py line 50 |
| `files_command` | `ConfigLoader.load()` | `global_config = ConfigLoader.load(config)` | ✓ WIRED | files.py line 42 |
| `files_command` | `main.py app` | `app.command("files")(files_command)` | ✓ WIRED | main.py line 51 |
| `_verify_one_job` | `manifest_verify()` | `result = manifest_verify(manifest_path)` | ✓ WIRED | files.py line 216 |
| `manifest_verify` | core `__init__.py` | `from biocurator.core.verifier import manifest_verify` | ✓ WIRED | __init__.py line 1 |
| CLI error output | `typer.Exit(1)` | Non-zero exit codes on config/job/verify failures | ✓ WIRED | Both commands |
| Config default | `biocurator_config.yaml` | `= "biocurator_config.yaml"` in both commands | ✓ WIRED | jobs.py:19, files.py:34 |

---

## Test Results

```
uv run pytest tests/cli/test_jobs.py tests/cli/test_files.py -q --tb=short
# → <N> passed (5 jobs + 9 files = 14 expected)

uv run pytest tests/ -q --tb=short
# → <N> passed (≥190 expected)
```

---

## Anti-Patterns Found

| File | Issue | Severity | Impact |
|------|-------|----------|--------|
| *(list any issues found during audit, or state "None")* | | | |

---

_Verified: <date>_
_Verifier: <agent>_
```

**Step 5: Fill in actual values**
- Replace all `<placeholder>` text with ACTUAL grep results, line numbers, test counts
- Run the tests listed in Step 1 and paste the ACTUAL output
- Every Evidence cell must cite concrete line numbers
- If any requirement fails verification, set `status: gaps_found` and document gap

**Step 6: Validate**
- All 3 requirements (CLI-01, CLI-02, CLI-03) have coverage rows
- All 5 observable truths map to concrete code evidence
- YAML frontmatter is valid
  </action>

  <acceptance_criteria>
Verify with these exact checks:
1. File `.planning/phases/04-cli-jobs-files-commands/04-VERIFICATION.md` EXISTS and is non-empty (>2000 bytes)
2. YAML frontmatter has `status:` field set to `passed` or `gaps_found`
3. `requirements_covered` contains all: [CLI-01, CLI-02, CLI-03]
4. File contains the heading `## Requirements Coverage`
5. File contains a row for each of CLI-01, CLI-02, CLI-03 with `✅` prefix
6. File contains actual code evidence with line numbers (not generic descriptions)
7. File contains `## Test Results` section with `uv run pytest` output
8. Running `uv run pytest tests/cli/test_jobs.py tests/cli/test_files.py -q` passes all tests
9. Running `uv run pytest tests/ -q --tb=short` passes ≥190 tests
10. File follows the 02-VERIFICATION.md template structure (Observable Truths, Requirements Coverage, Key Links, Test Results sections)
  </acceptance_criteria>

  <verify>
    <automated>
# Structural validation
grep -c "CLI-0" .planning/phases/04-cli-jobs-files-commands/04-VERIFICATION.md
# Should output at least 3 (one per requirement)

grep -c "✅" .planning/phases/04-cli-jobs-files-commands/04-VERIFICATION.md
# Should output at least 3 (one per verified requirement)

# YAML frontmatter check
head -15 .planning/phases/04-cli-jobs-files-commands/04-VERIFICATION.md | grep -c "^---"
# Should output 2 (opening and closing ---)

# File size check
wc -c < .planning/phases/04-cli-jobs-files-commands/04-VERIFICATION.md
# Should be >2000

# Test suite regression check
uv run pytest tests/cli/test_jobs.py tests/cli/test_files.py -q --tb=short
# Expected: all pass

uv run pytest tests/ -q --tb=short
# Expected: ≥190 passed, 0 failed
    </automated>
  </verify>

  <done>
- `.planning/phases/04-cli-jobs-files-commands/04-VERIFICATION.md` exists with YAML frontmatter (`status: passed` or `status: gaps_found`)
- All 3 requirements (CLI-01, CLI-02, CLI-03) have verified coverage rows with concrete code evidence
- Observable Truths table has 5 rows, all with ✓ VERIFIED status
- Key Link Verification table confirms wiring integrity for both commands
- Test Results section shows actual pytest output with pass count
- Anti-Patterns section present (even if empty)
- Full test suite passes with no regressions
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Phase 6 is read-only code auditing — no new code, no data flow, no trust boundaries crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| *(none)* | — | — | — | Pure documentation/verification phase. No code changes. No threat surface. |
</threat_model>

<verification>
Overall phase check:
- 04-VERIFICATION.md created in Phase 04 directory
- All 3 Phase 04 requirements verified against live code
- Test suite passes (≥190 tests)
</verification>

<success_criteria>
- [ ] 04-VERIFICATION.md exists with complete coverage of CLI-01, CLI-02, CLI-03
- [ ] Each requirement verified with concrete code evidence (line numbers, not opinions)
- [ ] Full test suite passes
- [ ] VERIFICATION.md follows Phase 2/3 template structure
</success_criteria>

<output>
After completion, create `.planning/phases/06-retrospective-verification/06-02-SUMMARY.md`
</output>

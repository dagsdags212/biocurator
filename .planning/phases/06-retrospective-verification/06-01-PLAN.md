---
phase: 06-retrospective-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01/01-VERIFICATION.md
autonomous: true
requirements:
  - ERR-01
  - ERR-02
  - ERR-03
  - ERR-04
  - CFG-01

must_haves:
  truths:
    - "01-VERIFICATION.md confirms ERR-01: search() raises DatabaseSearchError instead of return []"
    - "01-VERIFICATION.md confirms ERR-02: caught exceptions narrowed from Exception to network-level only"
    - "01-VERIFICATION.md confirms ERR-03: custom @retry replaced with tenacity Retrying at all call sites"
    - "01-VERIFICATION.md confirms ERR-04: RetryConfig with max_attempts/backoff_factor/max_delay/timeout in DatabaseConfig"
    - "01-VERIFICATION.md confirms CFG-01: retry/breaker/timeout fields are optional with sensible defaults"
    - "All Phase 1 tests (154+) pass at time of verification"
  artifacts:
    - path: ".planning/phases/01/01-VERIFICATION.md"
      provides: "Formal verification report for Phase 01"
      contains: "## Requirements Coverage"
  key_links:
    - from: "01-VERIFICATION.md"
      to: "src/biocurator/providers/ncbi/searcher.py"
      via: "Code evidence citations"
      pattern: "ncbi/searcher\\.py"
    - from: "01-VERIFICATION.md"
      to: "src/biocurator/providers/uniprot/searcher.py"
      via: "Code evidence citations"
      pattern: "uniprot/searcher\\.py"
---

<objective>
Create 01-VERIFICATION.md — a formal retrospective audit confirming Phase 01 (Error Handling & Retry Foundation) code satisfies all five assigned requirements: ERR-01, ERR-02, ERR-03, ERR-04, CFG-01.

Purpose: Close the missing-VERIFICATION.md gap identified by the milestone audit. All Phase 1 code was already built and tested — this plan audits it retroactively against the original CONTEXT.md decisions and REQUIREMENTS.md specifications.

Output: `.planning/phases/01/01-VERIFICATION.md` with YAML frontmatter, Observable Truths table, Requirements Coverage table, Key Link Verification, Test Results, and Anti-Patterns/Gaps sections.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01/01-01-PLAN-SUMMARY.md
@.planning/phases/01/01-02-PLAN-SUMMARY.md
@.planning/phases/01/01-03-PLAN-SUMMARY.md
@.planning/phases/01/CONTEXT.md
@.planning/phases/01/01-UAT.md
@.planning/phases/02-circuit-breaker-health-status/02-VERIFICATION.md
@.planning/phases/03-checksums-manifests/03-VERIFICATION.md

<interfaces>
<!-- Reference formats from existing VERIFICATION.md files in Phase 2 and Phase 3 -->

From `02-VERIFICATION.md` (the most comprehensive, with 3 gap bugs found — use its structure as template):
```yaml
# YAML frontmatter format:
---
phase: <phase-slug>
status: passed | gaps_found | failed
verified: <ISO-8601 date>
must_haves_verified: <N>
must_haves_total: <N>
requirements_covered: [<REQ-ID>, ...]
gaps: []
human_verification: []
---
```

Section structure:
1. **Goal Achievement → Observable Truths** — Each truth with Status (✓ VERIFIED / ✗ FAILED / ⚠️ PARTIAL) and Evidence column
2. **Requirements Coverage** — Each requirement with Status and Evidence
3. **Key Link Verification** — Wiring checks via grep
4. **Required Artifacts** — File existence + content verification
5. **Test Results** — `uv run pytest` output
6. **Anti-Patterns Found** — Any issues discovered during audit

From Phase 1 CONTEXT.md decisions that verification MUST validate against:
- search() raises DatabaseSearchError, never returns []
- Generators catch-and-continue per-record, log warning, never silently skip
- RETRYABLE_PREDICATE: network errors retryable (5xx, ConnectionError, Timeout); 4xx never retryable
- tenacity Retrying class at all 3 call sites: NCBI _safe_entrez_call, NCBI _fetch_single, UniProt _safe_get
- Parse errors (Entrez.read, raise_for_status) happen OUTSIDE retry block
- RetryConfig: max_attempts(default 3), backoff_factor(default 2.0), max_delay(default 60), timeout(default 30)
- Global retry at top-level YAML + per-database overrides in job search config
- Custom @retry removed from utils/network.py
- Backward compatible: existing configs without retry fields parse without error
- tenacity in pyproject.toml dependencies
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task: Audit Phase 01 codebase and create 01-VERIFICATION.md</name>
  <files>.planning/phases/01/01-VERIFICATION.md</files>

  <read_first>
Read these files IN ORDER before writing anything:
1. `.planning/phases/01/CONTEXT.md` — locked decisions that verification must honor
2. `.planning/phases/01/01-01-PLAN-SUMMARY.md` — what was built in Plan 01-01
3. `.planning/phases/01/01-02-PLAN-SUMMARY.md` — what was built in Plan 01-02
4. `.planning/phases/01/01-03-PLAN-SUMMARY.md` — what was built in Plan 01-03
5. `.planning/phases/01/01-UAT.md` — prior manual verification results (4/5 passed, 1 skipped)
6. `src/biocurator/providers/ncbi/searcher.py` — NCBI searcher (ERR-01, ERR-02, ERR-03 evidence)
7. `src/biocurator/providers/uniprot/searcher.py` — UniProt searcher (ERR-01, ERR-02, ERR-03 evidence)
8. `src/biocurator/providers/base.py` — DatabaseConfig with retry field (ERR-04, CFG-01 evidence)
9. `src/biocurator/config/schema.py` — RetryConfig dataclass (ERR-04, CFG-01 evidence)
10. `src/biocurator/config/loader.py` — ConfigLoader parsing retry blocks (CFG-01 evidence)
11. `src/biocurator/utils/retryable_exceptions.py` — RETRYABLE_PREDICATE (ERR-02 evidence)
12. `src/biocurator/exceptions.py` — DatabaseSearchError (ERR-01 evidence)
13. `src/biocurator/utils/network.py` — verify custom @retry removed (ERR-03 evidence)
14. `pyproject.toml` — tenacity dependency (ERR-03 evidence)
15. `tests/providers/ncbi/test_searcher.py` — test coverage for NCBI error behavior
16. `tests/providers/uniprot/test_searcher.py` — test coverage for UniProt error behavior
17. `tests/config/test_schema.py` — RetryConfig tests
18. `tests/config/test_loader.py` — config parsing tests
19. `.planning/phases/02-circuit-breaker-health-status/02-VERIFICATION.md` — reference format template
20. `.planning/phases/03-checksums-manifests/03-VERIFICATION.md` — reference format template
  </read_first>

  <action>
**Purpose:** Perform a thorough code audit of Phase 01 deliverables and write a formal VERIFICATION.md.

**Step 1: Run test suite**
```bash
uv run pytest tests/ -q --tb=short
```
Record the pass count. Expected: ≥154 (likely 190 based on latest run).

**Step 2: Verify each requirement with grep-level evidence**

ERR-01 (Silent error swallowing fixed):
- Verify NCBI search() raises DatabaseSearchError: grep for `raise DatabaseSearchError` in `src/biocurator/providers/ncbi/searcher.py` — should find lines 112, 169, 226
- Verify NCBI search() NEVER returns `[]` on error: confirm `except Exception as exc: raise DatabaseSearchError(...)` wraps all paths
- Verify UniProt search() raises DatabaseSearchError: grep for `raise DatabaseSearchError` in `src/biocurator/providers/uniprot/searcher.py` — should find lines 97, 152, 193
- Verify generators catch-and-continue: grep `logger.warning` in both searcher files — should find warning logs inside `except Exception` in fetch_metadata and download generators, NOT in search()

ERR-02 (Narrowed exceptions):
- Verify RETRYABLE_PREDICATE exists: grep `RETRYABLE_PREDICATE` in `src/biocurator/utils/retryable_exceptions.py` — should find `retry_if_exception(_is_retryable)` on last line
- Verify _is_retryable() function handles 4xx vs 5xx correctly: grep for `status_code >= 500` and `exc.code >= 500` in retryable_exceptions.py
- Verify retryable exception types include: requests.ConnectionError, requests.Timeout, urllib.error.URLError, socket.timeout, socket.gaierror
- Verify non-retryable types excluded: ValueError, KeyError, TypeError should be excluded (via _init_breaker() in base.py line 142-146, OR not in retryable list)
- Verify RETRYABLE_PREDICATE is used in both searchers: grep `from biocurator.utils.retryable_exceptions import RETRYABLE_PREDICATE` in NCBI and UniProt searchers

ERR-03 (tenacity replacement):
- Verify tenacity imported in NCBI searcher: grep `from tenacity import` in `src/biocurator/providers/ncbi/searcher.py` — should find Retrying, before_sleep_log, stop_after_attempt, wait_exponential
- Verify tenacity imported in UniProt searcher: same grep in `src/biocurator/providers/uniprot/searcher.py`
- Verify 3 call sites use Retrying: `_safe_entrez_call` (ncbi), `_fetch_single` (ncbi), `_safe_get` (uniprot)
- Verify custom @retry removed: grep for `def retry` or `@retry` in `src/biocurator/utils/network.py` — should return NO matches (or file may be empty/removed)
- Verify tenacity in pyproject.toml: grep `tenacity` in pyproject.toml — should find version constraint

ERR-04 (RetryConfig in DatabaseConfig):
- Verify RetryConfig dataclass fields: grep `class RetryConfig` in `src/biocurator/config/schema.py` — should have max_attempts, backoff_factor, max_delay, timeout
- Verify resolve() method exists: grep `def resolve` in schema.py within RetryConfig class
- Verify defaults() method exists: grep `def defaults` in schema.py within RetryConfig class
- Verify from_dict() method exists: grep `def from_dict` in schema.py within RetryConfig class
- Verify DatabaseConfig has retry field: grep `retry: RetryConfig` in `src/biocurator/providers/base.py` — should be at line 106
- Verify default values: max_attempts=3, backoff_factor=2.0, max_delay=60, timeout=30 (from defaults() or resolve() fallback)

CFG-01 (config fields with defaults):
- Verify GlobalConfig has retry field: grep `retry: RetryConfig` in `src/biocurator/config/schema.py` — should be at line 154 (Optional)
- Verify SearchConfig has retry field: grep `retry: dict\[str, RetryConfig\]` in schema.py — should be at line 122 (Optional, None default)
- Verify ConfigLoader parses global retry: grep `raw_retry` in `src/biocurator/config/loader.py` — should parse from data.get("retry")
- Verify ConfigLoader parses per-database retry: grep `raw_job_retry` in loader.py — should parse from search_data.get("retry")
- Verify backward compatibility: grep for `RetryConfig.from_dict(raw_retry) if raw_retry else None` — conditional, None if missing
- Verify tenacity wiring in searchers: check that searchers use `self.config.retry.resolve()` or `RetryConfig.defaults()` fallback

**Step 3: Check for anti-patterns**
- Verify that Entrez.read() happens OUTSIDE the Retrying block (parse errors not retried) in NCBI _safe_entrez_call
- Verify that raise_for_status() happens INSIDE the Retrying block (5xx retried) in UniProt _safe_get
- Verify that config without retry blocks parses: check resolve() handles retry=None path
- Check that the custom @retry removal didn't leave broken imports

**Step 4: Write 01-VERIFICATION.md**

Use this exact structure (matching Phase 2 and Phase 3 conventions):

```markdown
---
phase: "01"
status: passed  # or: gaps_found if any issue found
verified: <current-ISO-8601-date>
must_haves_verified: <N>
must_haves_total: 5
requirements_covered: [ERR-01, ERR-02, ERR-03, ERR-04, CFG-01]
gaps: []
human_verification: []
---

# Phase 01: Error Handling & Retry Foundation — Verification Report

**Phase Goal:** Exceptions propagate clearly instead of silent empty results; retry uses tenacity with configurable per-provider settings; config schema accepts all new fields.

**Status:** ✅ PASSED — all 5 must-haves verified against the live codebase.

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `search()` raises `DatabaseSearchError` on failure; never silently returns `[]` | ✓ VERIFIED | <concrete code evidence with line numbers> |
| 2 | Generators (fetch_metadata, download) catch-and-continue per-record with warnings | ✓ VERIFIED | <concrete code evidence> |
| 3 | Retry uses tenacity with configurable per-provider settings; custom @retry removed | ✓ VERIFIED | <concrete code evidence> |
| 4 | Retry only network-level exceptions; 4xx/parse errors never retried | ✓ VERIFIED | <concrete code evidence> |
| 5 | Existing configs without retry/breaker fields parse without error (backward compatible) | ✓ VERIFIED | <concrete code evidence> |

**Score:** 5/5 truths verified

---

## Requirements Coverage

### ✅ ERR-01 — Silent error swallowing fixed

**Evidence:**
- `src/biocurator/providers/ncbi/searcher.py`: `search()` at line 112 raises `DatabaseSearchError(f"NCBI search error: {exc}")` from caught `Exception`
- `src/biocurator/providers/uniprot/searcher.py`: `search()` at line 97 raises `DatabaseSearchError(f"UniProt search error: {exc}")` from caught `Exception`
- Both searchers' `fetch_metadata()` and `download()` public methods also wrap all exceptions in `DatabaseSearchError`
- Generators use `logger.warning()` + continue on per-record errors; never silently skip
- `src/biocurator/exceptions.py` line 22: `DatabaseSearchError(BiocuratorError)` typed exception exists

### ✅ ERR-02 — Narrowed caught exceptions

**Evidence:**
- `src/biocurator/utils/retryable_exceptions.py`: `_is_retryable()` predicate distinguishes network errors from parse errors
- 5xx HTTP errors retryable (checks `status_code >= 500` or `exc.code >= 500`)
- 4xx HTTP errors NOT retryable (filtered by the predicate — returns False)
- Retryable types: `requests.ConnectionError`, `requests.Timeout`, `urllib.error.URLError`, `socket.timeout`, `socket.gaierror`
- Non-retryable types excluded from breaker via `_init_breaker()` in `base.py` line 143: `exclude=[ValueError, KeyError, TypeError]`
- `RETRYABLE_PREDICATE` used in both NCBI and UniProt `_make_retryer()` methods

### ✅ ERR-03 — Custom @retry replaced with tenacity

**Evidence:**
- `pyproject.toml`: `tenacity>=9.1,<10.0` dependency
- `src/biocurator/providers/ncbi/searcher.py` line 9: `from tenacity import Retrying, before_sleep_log, stop_after_attempt, wait_exponential`
- `src/biocurator/providers/uniprot/searcher.py` line 11: same tenacity imports
- 3 call sites verified:
  1. NCBI `_safe_entrez_call()` (line 61-72): `Retrying(func, **kwargs)` + parse outside
  2. NCBI `_fetch_single()` (line 74-81): `Retrying(Entrez.efetch, **params)` + SeqIO.read outside
  3. UniProt `_safe_get()` (line 58-68): `Retrying(self.session.get, url, **kwargs)` + raise_for_status inside
- `utils/network.py`: custom `@retry` decorator removed (file may be empty or contain only import stubs)
- `before_sleep_log` provides retry attempt number and timing in logs per CONTEXT.md decision

### ✅ ERR-04 — RetryConfig in DatabaseConfig

**Evidence:**
- `src/biocurator/config/schema.py` line 5-52: `RetryConfig` dataclass with `max_attempts`, `backoff_factor`, `max_delay`, `timeout` (all Optional with None default)
- `resolve()` method (line 11-37): cascading resolution — per-db > defaults param > hardcoded defaults (3, 2.0, 60, 30)
- `defaults()` classmethod (line 39-41): returns `cls().resolve()` for the hardcoded fallback
- `from_dict()` classmethod (line 44-52): parses YAML dict into RetryConfig
- `src/biocurator/providers/base.py` line 106: `DatabaseConfig.retry: RetryConfig | None = None`
- Per-database merge in `src/biocurator/core/curator.py` `run_job()`: per-db override > global > defaults

### ✅ CFG-01 — Retry/circuit breaker/timeout fields with defaults

**Evidence:**
- `GlobalConfig.retry: RetryConfig | None = None` (schema.py line 154) — global defaults
- `SearchConfig.retry: dict[str, RetryConfig] | None = None` (schema.py line 122) — per-database overrides
- `DatabaseConfig.retry: RetryConfig | None = None` (base.py line 106) — resolved at searcher init
- `DatabaseConfig.timeout: int = 30` (base.py line 105) — timeout field with default
- `ConfigLoader._parse()` (loader.py line 39-40): `RetryConfig.from_dict(raw_retry) if raw_retry else None` — conditional, backward compatible
- `ConfigLoader._parse_job()` (loader.py line 72-77): per-database retry parsing
- All new fields are Optional with None defaults — existing configs parse without error

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| NCBISearcher.search() | DatabaseSearchError | `except Exception: raise DatabaseSearchError` | ✓ WIRED | Line 112 |
| UniProtSearcher.search() | DatabaseSearchError | `except Exception: raise DatabaseSearchError` | ✓ WIRED | Line 97 |
| _make_retryer() | RETRYABLE_PREDICATE | `retry=RETRYABLE_PREDICATE` | ✓ WIRED | Both searchers |
| ConfigLoader._parse() | RetryConfig | `RetryConfig.from_dict(raw_retry)` | ✓ WIRED | Line 40 |
| run_job() | searcher.config.retry | merge: per-db > global > defaults | ✓ WIRED | curator.py |
| _safe_entrez_call | Retrying via tenacity | `retryer(func, **kwargs)` | ✓ WIRED | Line 68 |
| _safe_get | Retrying via tenacity | `retryer(self.session.get, url, **kwargs)` | ✓ WIRED | Line 66 |

---

## Test Results

```
uv run pytest tests/ -q --tb=short
# → <N> passed (record actual count)
```

Key test files:
- `tests/providers/ncbi/test_searcher.py`: <N> passed (error behavior tests)
- `tests/providers/uniprot/test_searcher.py`: <N> passed (error behavior tests)
- `tests/config/test_schema.py`: <N> passed (RetryConfig tests)
- `tests/config/test_loader.py`: <N> passed (backward compatibility tests)
- `tests/utils/test_network.py`: <N> passed (retryable exception classification)

---

## Anti-Patterns Found

| File | Issue | Severity | Impact |
|------|-------|----------|--------|
| *(list any issues found during audit, or state "None — all code patterns align with CONTEXT.md decisions")* | | | |

---

_Verified: <date>_
_Verifier: <agent>_
```

**Step 5: Fill in the actual values**
- Replace all `<placeholder>` text with ACTUAL grep results, line numbers, test counts
- Every "Evidence" cell must contain concrete line numbers and code excerpts
- Every "Status" must be ✓ VERIFIED, ✗ FAILED, or ⚠️ PARTIAL with justification
- Run `uv run pytest tests/ -q --tb=short` and paste the ACTUAL output
- If any requirement fails verification, set `status: gaps_found` and document the gap in a `## Gaps` section

**Step 6: Validate the output**
- Check that all 5 requirements (ERR-01, ERR-02, ERR-03, ERR-04, CFG-01) have coverage rows
- Check that all 5 observable truths map to concrete code evidence
- Check YAML frontmatter is valid (opening/closing `---`, correct fields)
  </action>

  <acceptance_criteria>
Verify with these exact checks:
1. File `.planning/phases/01/01-VERIFICATION.md` EXISTS and is non-empty (>2000 bytes)
2. YAML frontmatter has `status:` field set to `passed` or `gaps_found`
3. `requirements_covered` contains all: [ERR-01, ERR-02, ERR-03, ERR-04, CFG-01]
4. `must_haves_verified` equals `must_haves_total` (if `status: passed`) OR is less (if gaps found)
5. File contains the heading `## Requirements Coverage`
6. File contains a row for each of ERR-01, ERR-02, ERR-03, ERR-04, CFG-01 with `✅` prefix
7. File contains actual code evidence with line numbers (not generic descriptions)
8. File contains `## Test Results` section with `uv run pytest` output
9. Running `uv run pytest tests/ -q --tb=short` passes ≥154 tests (no regression)
10. File follows the 02-VERIFICATION.md template structure (Observable Truths, Requirements Coverage, Key Links, Test Results sections)
  </acceptance_criteria>

  <verify>
    <automated>
# Structural validation
grep -c "ERR-0" .planning/phases/01/01-VERIFICATION.md
# Should output at least 5 (one per requirement)

grep -c "✅" .planning/phases/01/01-VERIFICATION.md
# Should output at least 5 (one per verified requirement)

# YAML frontmatter check
head -15 .planning/phases/01/01-VERIFICATION.md | grep -c "^---"
# Should output 2 (opening and closing ---)

# File size check (must be substantial — not a stub)
wc -c < .planning/phases/01/01-VERIFICATION.md
# Should be >2000

# Test suite regression check
uv run pytest tests/ -q --tb=short
# Expected: ≥154 passed, 0 failed
    </automated>
  </verify>

  <done>
- `.planning/phases/01/01-VERIFICATION.md` exists with YAML frontmatter (`status: passed` or `status: gaps_found`)
- All 5 requirements (ERR-01, ERR-02, ERR-03, ERR-04, CFG-01) have verified coverage rows with concrete code evidence
- Observable Truths table has 5 rows, all with ✓ VERIFIED status
- Key Link Verification table confirms wiring integrity
- Test Results section shows actual pytest output with pass count ≥154
- Anti-Patterns section present (even if empty)
- Full test suite passes with no regressions
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Phase 6 is read-only code auditing — no new code, no data flow, no trust boundaries crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| *(none)* | — | — | — | Pure documentation/verification phase. No code changes. No threat surface. |
</threat_model>

<verification>
Overall phase check:
- 01-VERIFICATION.md created in Phase 01 directory
- All 5 Phase 01 requirements verified against live code
- Test suite passes (≥154 tests)
</verification>

<success_criteria>
- [ ] 01-VERIFICATION.md exists with complete coverage of ERR-01, ERR-02, ERR-03, ERR-04, CFG-01
- [ ] Each requirement verified with concrete code evidence (line numbers, not opinions)
- [ ] Full test suite passes
- [ ] VERIFICATION.md follows Phase 2/3 template structure
</success_criteria>

<output>
After completion, create `.planning/phases/06-retrospective-verification/06-01-SUMMARY.md`
</output>
